# hot_reload.py 的本地状态文件（记录上次扫描的 mtime）
tools/.hot_reload_state.json

# server.py 单实例锁文件（运行时生成）
server.pid

# sync.py 是个人同步工具（含个人路径配置），不上传
sync.py

# HANDOVER.md 含 token/邮箱/绝对路径等敏感信息，仅本地用，不上传
HANDOVER.md

# Python 缓存
__pycache__/
*.pyc
*.pyo

# 编辑器
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store
Thumbs.db

# 本地数据源构建脚本（含个人反编译路径，依赖本地 3.9反编译 环境，不上传）
tools/build_index_private.py

# 优化备份文件（本地对比用）
*.bak
